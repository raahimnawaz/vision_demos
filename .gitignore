# Byte-compiled / cache
__pycache__/
*.py[cod]
.pytest_cache/

# Virtual environments
*env/
.venv/

# Model weights & downloaded assets
# (YOLO weights auto-download via ultralytics; .task bundles via download_models.sh)
*.pt
models/*.task

# Runtime outputs
shots/
testimg/out_*

# colcon build artifacts (ros2/ is a workspace; only src/ is tracked)
ros2/build/
ros2/install/
ros2/log/

# rosbags
*.db3
*.mcap
rosbag2_*/

# OS / editor cruft
.DS_Store
.idea/
.vscode/
